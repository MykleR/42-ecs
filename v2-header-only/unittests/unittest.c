#include <utest.h>
#include <ecs_utils.h>


__attribute__((constructor))
void init() { LOG_INFO("STARTING\n");}

UTEST(ecs_vec, init) {
	UTEST_SKIP("Need to implement this test!");
}

UTEST(ecs_vec, push) {
	UTEST_SKIP("Need to implement this test!");
}

UTEST(ecs_vec, pop) {
	UTEST_SKIP("Need to implement this test!");
}

UTEST(ecs_vec, resize) {
	UTEST_SKIP("Need to implement this test!");
}

UTEST(ecs_vec, clear) {
	UTEST_SKIP("Need to implement this test!");
}

UTEST(ecs_vec, free) {
	UTEST_SKIP("Need to implement this test!");
}

UTEST(ecs_vec, iter) {
	UTEST_SKIP("Need to implement this test!");
}

UTEST_MAIN();
